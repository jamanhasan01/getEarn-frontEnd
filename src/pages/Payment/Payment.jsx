import { Elements } from "@stripe/react-stripe-js";
import { loadStripe } from "@stripe/stripe-js";
import CheckoutForm from "./CheckoutForm";


const Payment = ({setshowmodel}) => {
  return (
    <div>
     
    </div>
  );
};

export default Payment;
